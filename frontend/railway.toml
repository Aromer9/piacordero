# Build con Docker (Node 20) para evitar fallos de npm ci / versión de Node en Railpack.
# Root Directory del servicio debe ser: frontend

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
