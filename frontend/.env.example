# Producción (Railway): URL pública del API, sin /api al final.
# Ejemplo: https://piacordero-api.up.railway.app
# En local déjalo vacío: Vite usa el proxy de vite.config.js hacia el backend.
# VITE_BACKEND_ORIGIN=
